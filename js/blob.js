(() => {

    const config = {
        size: 30,
        color: `rgba(85, 255, 235, 0.9)`,
        speed: 1,
    }

    const canvas = document.getElementById(`blob-canvas`);
    const ctx = canvas.getContext(`2d`);
    
    function createBlob() {
        ctx.fillRect = "blue"
    }
})